# `/prove-missing` Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Build a `/prove-missing` Claude Code skill that generates a structured informal proof via WebSearch before driving Lean 4 formalization, for theorems whose proofs are absent from the Bubeck book.

**Architecture:** New skill file `.claude/commands/prove-missing.md` modeled on the existing `prove-theorem.md`, with a WebSearch + synthesis step inserted between lookup and autoprove. The informal proof is saved to `proofs/<book>/informal/<TheoremName>_informal.md` and passed as context to `lean4:autoprove`.

**Tech Stack:** Claude Code skill files (Markdown), existing `scripts/lookup_theorem.py`, `scripts/update_theorem_status.py`, `lean4:autoprove`, WebSearch tool.

---

### Task 1: Create the `informal/` directory and a README

**Files:**
- Create: `proofs/Bubeck_convex_optimization/informal/.gitkeep`
- Create: `proofs/Bubeck_convex_optimization/informal/README.md`

**Step 1: Create the directory with a gitkeep**

```bash
mkdir -p "proofs/Bubeck_convex_optimization/informal"
touch "proofs/Bubeck_convex_optimization/informal/.gitkeep"
```

**Step 2: Write the README**

Create `proofs/Bubeck_convex_optimization/informal/README.md` with:

```markdown
# Informal Proofs

This directory contains structured informal proofs generated by the `/prove-missing`
workflow for theorems that are cited without proof in the Bubeck book.

Each file is named `<TheoremName>_informal.md` and contains:
- Theorem statement
- Sources found via WebSearch
- Step-by-step proof (English + Math + Lean/Mathlib hints)
- Mathlib gaps identified during formalization
- Status: PROOF FOUND | CLAUDE-GENERATED | PROOF NOT FOUND
```

**Step 3: Verify directory exists**

```bash
ls proofs/Bubeck_convex_optimization/informal/
```

Expected: `.gitkeep  README.md`

**Step 4: Commit**

```bash
git add proofs/Bubeck_convex_optimization/informal/
git commit -m "feat: add informal/ directory for prove-missing workflow"
```

---

### Task 2: Write the `/prove-missing` skill file

**Files:**
- Create: `.claude/commands/prove-missing.md`
- Reference: `.claude/commands/prove-theorem.md` (existing — do not modify)

**Step 1: Read the existing `prove-theorem.md` for style reference**

Read `.claude/commands/prove-theorem.md` to understand the exact structure (step numbering, bash snippets, Skill invocation pattern). The new skill mirrors it exactly except for the inserted informal-proof steps.

**Step 2: Write `.claude/commands/prove-missing.md`**

Create the file with the following content — copy prove-theorem.md as the base and insert Steps 3–4 (WebSearch + synthesis) between the existing build step and the example-proof step:

```markdown
Look up a theorem by ID, generate a structured informal proof via WebSearch,
then formalize it with lean4:autoprove. Use this for theorems whose proofs
are missing from the book (cited without proof).

> **Run autonomously.** Do not ask the user for confirmations, approvals,
> or plan reviews at any step. Proceed directly through all steps without pausing.

## Steps

### 0. Record start time

\```bash
PROVE_START=$(python3 -c "import time; print(time.time())")
\```

### 1. Lookup

Run:
\```bash
python3 scripts/lookup_theorem.py "$ARGUMENTS"
\```

Parse the JSON output. If exit code is non-zero, stop and show the error.
Note the values: `book`, `theorem_id`, `theorem_name`, `section_file`,
`lean_file`, `example_proof_file`.

### 2. Build

Run:
\```bash
lake exe cache get && lake build
\```

If this fails, stop and report the build error.

### 3. WebSearch for informal proof

Run the following queries in sequence, stopping early if a high-quality
proof is found (e.g., an existing Lean/Mathlib formalization):

1. `"<theorem_name> proof"` — direct search for classical proof
2. `"<theorem_name> convex geometry proof sketch"` — broaden if sparse
3. `"<theorem_name> Lean 4 Mathlib formalization"` — check if already done
4. `"<key mathematical ingredient> Mathlib"` — verify supporting lemmas
5. `"<theorem_name> lecture notes"` — fallback for pedagogical write-ups

**Source priority** (highest → lowest):
1. Existing Lean 4 / Mathlib formalizations → use directly, skip to Step 6
2. Wikipedia
3. arXiv lecture notes / survey papers
4. Math StackExchange

If no useful sources are found after all 5 queries, proceed with Claude's
own mathematical knowledge and flag the result as CLAUDE-GENERATED.

### 4. Synthesize and save informal proof

Determine the output path:
\```
INFORMAL_DIR="proofs/<book>/informal"
INFORMAL_FILE="$INFORMAL_DIR/<TheoremName>_informal.md"
\```
(Use the `book` and `theorem_id` values from Step 1 to derive the filename,
e.g. `Lemma22_informal.md` for theorem_id `Lemma 2.2`.)

Write the file using this template — fill every field from the sources found:

\```markdown
# Informal Proof: <theorem_name>

## Sources
- [Source title](url)

## Theorem Statement
<Full statement in plain English + math notation>

## Proof Steps

### Step N: <Title>

**English:** <Full sentence explanation of what this step does and why.>

**Math:** <The key equation or inequality being established, with symbols.>

**Lean/Mathlib hint:** <Candidate lemma names or search terms for lean_leansearch>

**Sorry prediction:** yes/no — yes if this step needs `sorry` due to missing Mathlib lemma

## Mathlib Gaps
- <Lemma description>: not found in Mathlib 4.x (search confirmed)

## Status
PROOF FOUND | CLAUDE-GENERATED (unverified) | PROOF NOT FOUND — manual input needed
\```

If no proof could be generated (Status = PROOF NOT FOUND):
- Save the minimal file with the Status line only
- Skip Steps 5–7
- Jump to Step 8 with status = `failed`

### 5. Read example proof

Read the file at `example_proof_file` from the lookup JSON for style reference:
- Which Optlib and Mathlib modules are imported
- How the `variable` block is structured
- How `open` statements are used
- Overall theorem structure

### 6. Construct autoprove prompt

Build the following prompt (fill `<...>` from lookup JSON and the informal proof):

\```
Formalize <theorem_id> (<theorem_name>) from:
    <section_file>

The statement is:
<theorem_statement>

Informal proof (from <INFORMAL_FILE>):
<paste full contents of _informal.md here>

Steps:
1. Work through the informal proof steps in order. For each step:
   a. Translate the Math field into a Lean sub-goal or intermediate lemma.
   b. Use the Lean/Mathlib hint to search with lean_leanfinder / lean_leansearch.
   c. If Sorry prediction = yes, place `sorry` with a comment:
      -- TODO: needs <hint> — not yet in Mathlib
      and continue to the next step.
2. Search for relevant lemmas in this order:
   a. Previously proved theorems in proofs/<book>/ — list the directory,
      read relevant files, note theorem names and signatures.
      Import with: import proofs.<book>.<FileName>
   b. Optlib (.lake/packages/optlib/Optlib/) — key modules:
      Optlib/Convex/Subgradient.lean, ConvexFunction.lean
   c. Mathlib — search with lean_leanfinder / lean_leansearch
3. See <example_proof_file> for expected style.
4. Create/update <lean_file> implementing the proof.
5. Run /lean4:checkpoint when the proof compiles (even with sorrys).
6. Do NOT update index.md or logs/proofs.log — prove-missing handles that.
\```

### 7. Invoke autoprove

Use the Skill tool to invoke `lean4:autoprove` with the prompt from Step 6.

### 8. Post-proof

**If status = `proved` (no sorrys):**
\```bash
ELAPSED=$(python3 -c "import time; e=int(time.time()-$PROVE_START); print(f'{e//60}m {e%60}s')")
python3 scripts/update_theorem_status.py "$ARGUMENTS" proved --time "$ELAPSED"
\```

**If status = `partial` (sorrys remain for Mathlib gaps):**
\```bash
python3 scripts/update_theorem_status.py "$ARGUMENTS" partial
\```

**If status = `failed` (proof not found or autoprove failed):**
\```bash
python3 scripts/update_theorem_status.py "$ARGUMENTS" failed
\```

Append an entry to `logs/proofs.log`:
\```python
import json, datetime
entry = {
  "ts": datetime.datetime.now().isoformat(timespec="seconds"),
  "event": "proof_session",
  "source": "<section_file>",
  "theorem": "<theorem_id>",
  "lean_file": "<lean_file>",
  "informal_proof_file": "<INFORMAL_FILE>",
  "status": "<proved|partial|failed>",
  "notes": "<brief description: sources used, Mathlib gaps found>"
}
with open("logs/proofs.log", "a") as f:
    f.write(json.dumps(entry) + "\n")
\```

### 9. Report

Summarise:
- Theorem proved/attempted
- Informal proof file created (path + status)
- Lean file created/updated
- Updated status in index.md
- Mathlib gaps identified (if any)
- Any notable lemmas or techniques used
```

**Step 3: Verify the file was created**

```bash
ls -la .claude/commands/prove-missing.md
```

Expected: file exists, non-zero size.

**Step 4: Commit**

```bash
git add .claude/commands/prove-missing.md
git commit -m "feat: add /prove-missing skill for theorems with missing proofs"
```

---

### Task 3: Dry-run validation against Lemma 2.2

This task validates the skill's logic without running autoprove — just the lookup, WebSearch, and synthesis steps.

**Files:**
- Read: `proofs/Bubeck_convex_optimization/Lemma22.lean`
- Read: `papers/Bubeck_convex_optimization/sections/02_01_the_center_of_gravity_method.md`
- Write: `proofs/Bubeck_convex_optimization/informal/Lemma22_informal.md`

**Step 1: Run the lookup**

```bash
python3 scripts/lookup_theorem.py "Lemma 2.2"
```

Expected JSON output includes: `book`, `theorem_id`, `section_file`, `lean_file`.
If this fails, check that `Lemma 2.2` is registered in the index — it should be in `papers/Bubeck_convex_optimization/index.md`.

**Step 2: Run WebSearch manually (5 queries)**

Use the WebSearch tool with:
1. `"Grunbaum theorem proof convex geometry"`
2. `"Grunbaum 1960 convex body centroid halfspace volume proof"`
3. `"Grunbaum theorem Lean 4 Mathlib formalization"`
4. `"Brunn-Minkowski inequality Mathlib4"`
5. `"Grunbaum theorem lecture notes proof sketch"`

Collect sources and assess quality.

**Step 3: Write `Lemma22_informal.md`**

Based on search results, write the full informal proof file to
`proofs/Bubeck_convex_optimization/informal/Lemma22_informal.md`
covering all 4 proof steps (affine invariance → Brunn-Minkowski slicing
→ 1D reduction → `(1-1/n)^n ≥ 1/e`), with English + Math + Lean hints.

**Step 4: Verify the file is well-formed**

Check that the file:
- Has all required sections (Sources, Theorem Statement, Proof Steps, Status)
- Each step has English + Math + Lean/Mathlib hint + Sorry prediction
- Status line is one of the three valid values

**Step 5: Commit the informal proof file**

```bash
git add proofs/Bubeck_convex_optimization/informal/Lemma22_informal.md
git commit -m "feat(Lemma22): add informal proof via WebSearch (Grunbaum 1960)"
```

---

### Task 4: Full end-to-end run on Lemma 2.2

Run the complete `/prove-missing` skill on Lemma 2.2 to validate the full pipeline.

**Files:**
- Modify: `proofs/Bubeck_convex_optimization/Lemma22.lean`
- Modify: `logs/proofs.log`
- Modify: `papers/Bubeck_convex_optimization/index.md`

**Step 1: Invoke the skill**

```
/prove-missing "Lemma 2.2"
```

The skill will:
- Re-use the existing `Lemma22_informal.md` from Task 3 (no re-search needed)
- Invoke `lean4:autoprove` with the informal proof as context
- Update status and log

**Step 2: Verify Lean file compiles**

```bash
lake env lean proofs/Bubeck_convex_optimization/Lemma22.lean
```

Expected: zero errors. Warnings about `sorry` are acceptable (partial status).

**Step 3: Verify log entry**

```bash
tail -1 logs/proofs.log | python3 -m json.tool
```

Expected: valid JSON with `theorem: "Lemma 2.2"` and `status: "proved"` or `"partial"`.

**Step 4: Verify index.md updated**

```bash
grep "Lemma 2.2" papers/Bubeck_convex_optimization/index.md
```

Expected: status column shows `proved` or `partial` (not `pending`).

**Step 5: Checkpoint**

```bash
/lean4:checkpoint
```

Or manually:
```bash
git add proofs/Bubeck_convex_optimization/Lemma22.lean logs/proofs.log papers/Bubeck_convex_optimization/index.md
git commit -m "checkpoint(lean4): prove Lemma 2.2 Grunbaum theorem (best-effort)"
```

---

## Notes for the Implementer

- The skill file uses `\``` ` (escaped backticks) in the plan above to avoid markdown nesting — write actual triple backticks in the real `.claude/commands/prove-missing.md`
- `$ARGUMENTS` in skill files is the string passed after the slash command (e.g. `"Lemma 2.2"`)
- The `scripts/lookup_theorem.py` script returns JSON — parse with `import json; data = json.loads(output)`
- If `lake build` takes too long (first run can be 5–10 min), use `lake exe cache get` first to download prebuilt `.olean` files
- Grünbaum's theorem (Lemma 2.2) will almost certainly remain `partial` — the Brunn-Minkowski inequality in the form needed is not currently in Mathlib 4. This is expected and correct behavior.
